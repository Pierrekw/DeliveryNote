# Delivery Note HTM 解析器

用于将 Docs 文件夹下的 htm 格式送货单文件转换为 JSON 格式的工具。

## 功能特性
- 解析 HTM 格式的 Delivery Note 文件
- 提取关键信息（DN 号、日期、物料号、数量等）
- 转换为结构化的 JSON 格式输出
- 支持批量处理多个文件

## 环境要求
- Python 3.6+
- 依赖包见 requirements.txt

## 安装依赖
```
pip install -r requirements.txt
```

## 使用方法

### 方式1: 直接运行脚本
```
python htm_parser.py
```
这将:
- 自动扫描 Input/ 目录下所有 htm/html 文件
- 解析每个文件并提取信息
- 在 Output/ 目录生成 xxx.json 文件

### 方式2: 作为模块使用
```python
from htm_parser import DeliveryNoteParser, process_all_htm_files
# 解析单个文件
parser = DeliveryNoteParser()
result = parser.parse_htm_file('path/to/file.htm')
# 批量处理
process_all_htm_files('Docs', 'output.json')
```

## 输出格式

生成的 JSON 文件包含以下字段:
```json
{
  "file_name": "文件名",
  "DN号": "送货单号",
  "日期": "日期",
  "客户代码": "客户代码",
  "客户编号": "客户编号",
  "客户物料号": "客户物料号",
  "送货地址": "送货地址",
  "收货人": "收货人",
  "联系电话": "联系电话",
  "ZF物料号": "ZF物料号",
  "零件号": "零件号",
  "名字": "产品名称",
  "数量": "数量",
  "净重": "净重",
  "毛重": "毛重",
  "包装": "包装信息",
  "项目": [
    {
      "raw": "原始数据"
    }
  ]
}
```

## 参考文件
- Docs/DN内容.xlsx: 定义了需要提取的字段结构

## 目录结构
```
DeliveryNote/
├── Docs/                      # 存放HTM源文件
│   ├── Delivery Note 78018807 .htm
│   └── DN内容.xlsx
├── htm_parser.py             # 主解析脚本
├── requirements.txt          # Python依赖
├── delivery_notes.json       # 输出的JSON文件(生成)
└── README.md                # 说明文档
```

## 开发说明

解析器使用 BeautifulSoup 库解析 HTML,通过正则表达式提取需要的字段。
如果需要调整提取逻辑,可以修改 htm_parser.py 中对应的 _extract_* 方法。

## License

MIT
20260423

---

# Delivery Note HTM → JSON Parser

一个用于解析 **SAP / ZF Delivery Note（HTM 格式）** 的 Python 解析器，
目标是在 **模板不一致、版式变化、字段缺失、订单跨行** 的情况下，
仍然能够 **稳定、可维护地输出结构化 JSON**。

## ✨ 特性概览
- ✅ 支持 HTM / HTML 格式 Delivery Note
- ✅ 支持多页（Page 1/2, 2/2）
- ✅ 支持项目多行结构
- ✅ 支持订单信息跨行（如 6275 模板）
- ✅ 支持单空格压缩订单行
- ✅ 以 colli 作为项目边界，避免误解析页脚/银行信息
- ✅ 自动清洗 &nbsp;、多余空格
- ✅ Debug / Trace 模式，方便排错和回归验证

## 📂 输入 / 输出

### 输入
- Docs/ 目录下的 .htm / .html Delivery Note 文件

### 输出
- Output/ 目录下与源文件同名的 .json
- UTF-8 编码，字段已清洗

## 🧠 核心设计思想

### 1️⃣ 双轨文本处理（非常重要）

解析过程中维护 **两套文本行**：

| 变量 | 用途 |
|---|---|
| raw_lines | **保留原始空格与版式**，用于项目表格解析 |
| lines | **压缩空格**，用于备注 / 地址 / 联系人等语义解析 |

❗ **绝对不要用压缩过空格的文本去解析表格结构**

### 2️⃣ 项目（Item）区块的结构模型

每一个项目遵循如下逻辑结构：
```
项目主行
订单信息子行（1 行或多行）
日期行
colli 行（项目结束标志）
```

示例（6275 模板）：
```
010 4410441020 ECAS-Pressure Sensor, CVDE CN 39EA
11050926-00 2301400308HZ00(04.24 115797118
30.01.2026
colli: 174151807 39 EA
```

### 3️⃣ 项目边界规则（核心）
- ✅ **一个项目从 ITEM 行开始**
- ✅ **遇到 colli 行立即结束当前项目**
- ❌ colli 之后的内容（包装号、银行账号、页脚）一律不参与项目解析

这条规则用于避免：
- 把包装号当成订单号
- 把银行账号当成零件号
- 把页脚污染项目数据

### 4️⃣ 项目主行字段解析规则

#### 表头语义
```
项目号 | 零件号 | 名字 | Country of origin | 数量
```

#### 实现规则
- **数量**：匹配 `\d+EA`
- **国家**：匹配任意 `\b[A-Z]{2}\b`（CN / DE / TR / …）
- **名字**：
  - 优先：零件号之后 → 国家之前
  - fallback：零件号之后 → EA 之前

✅ 国家不写死
✅ 名字不再包含 CN / DE 等国家码

### 5️⃣ 订单信息解析（最复杂、最关键）

订单信息存在多种真实形态：

#### ✅ 表格对齐（多空格）
```
11050926-00    2301400308HZ00(04.24    115797118
```

#### ✅ 单空格压缩（6275）
```
11050926-00 2301400308HZ00(04.24 115797118
```

#### ✅ 两字段结构
```
LSDY-2026-4-10 115871821
```

#### ✅ 统一解析策略
- **在项目子区块内持续扫描，直到拿到 Our Order No.**
- 优先使用 **多空格分列**
- fallback：整行扫描 ≥6 位数字
- **从右向左识别**：
  - 最右侧 ≥6 位纯数字 → Our Order No.
  - 左侧依次回填：贵方订单号、贵方零件号

### 6️⃣ 文本清洗策略

所有字段在写入 JSON 前都会经过统一清洗：
- &nbsp; / \xa0 → 普通空格
- 多空格压缩
- 首尾空格去除

示例：`Clutch Servo _AM  CN` → `Clutch Servo _AM`

## 🐞 Debug / Trace 模式

### 启用方式
```python
parser = DeliveryNoteParser(path, config, debug=True)
```

### 示例输出
```
[TRACE] 项目 010：订单号 115797118 来自第 2 行（单空格 fallback）
[TRACE] 项目 010：遇到 colli，结束项目扫描
```

用途：
- 快速定位订单来源
- 回归测试验证解析路径
- 新模板调试

## 📌 已验证模板
- ✅ 标准 SAP 表格模板
- ✅ 多页 Delivery Note
- ✅ 6275（单空格压缩订单行）
- ✅ 78196275（多行订单 + colli 边界）
- ✅ 索赔 / 免费订单
- ✅ 中英文混合名称

## 🧪 建议回归用例
- 6275（单空格订单行）
- 78196275（多行订单 + colli）
- 77535479（三字段订单）
- 正常单页 / 多页 Delivery Note

## 🏁 总结

**这是一个"结构驱动 + 语义兜底"的解析器。** 不追求模板完全一致，而依赖稳定的结构事实：
- 项目以 ITEM 开始
- 项目以 colli 结束
- Our Order No. 永远是纯数字

## 📬 后续可扩展方向
- 抽象 ItemBlockParser 类
- 输出 Country of origin 字段
- 单元测试（pytest）
- Debug 输出切换为 logging

**Author**: Pierre　**Status**: Production-ready ✅

---

# 更新日志（2026-08-26）

> **主题**：修复项目明细解析的多类 Bug、新增回归工具、新增 `move_to_bak` 开关、整理打包流程
> **涉及文件**：`htm_parser.py`（原 `htm_parser_fixed.py`）、`config.yaml`、`regression_check.py`、`build.bat`
> **状态**：已自测通过，待打包发布 ✅

## 一、本次工作总览

| 类别 | 内容 | 状态 |
|---|---|---|
| 🐞 Bug 修复 | 项目明细解析 5 类问题 + 3 个二次回归 | ✅ 完成 |
| 🧪 新增工具 | `regression_check.py` 回归对比工具（自动定位路径） | ✅ 完成 |
| ⚙️ 新增配置 | `config.yaml` 增加 `move_to_bak` 开关 | ✅ 完成并自测 |
| 📦 打包 | 确认 `build.bat` + PyInstaller 流程 | ✅ 已梳理 |
| ✔️ 验证 | 新批次 16 单据 / 91 行 + 老样本 5 个双向回归 | ✅ 全部通过 |

## 二、解析器 Bug 修复（核心）

所有改动集中在**项目明细解析**（`_parse_items()` + 顶部正则 + 新增 `_item_col_x()`），
文件其余部分（表头/公司/收货/备注解析、日志、CLI、main）**未改动**。

### 2.1 根因总结

旧代码依赖两个僵硬假设：
1. **列与列之间一定有 ≥2 个空格**（`re.split(r"\s{2,}")`）
2. **Our Order No. 一定是"纯数字整列"**（`fullmatch(r"\d{6,}")`）

真实单据一旦出现 **列贴列、中文粘数字、名字跨行、小数数量**，就会失配或错配。
新版改为 **"特征锚定 + 列坐标判定"**，对排版噪声容错。

### 2.2 修复清单

| # | 问题现象 | 触发数据特征 | 根因（旧代码） | 修复方法 |
|---|---|---|---|---|
| ① | 物料名字解析不完整 | 名字被 HTML 换行拆开，或夹 `--`；名字尾部粘国家码（DynamicHU/24VCN） | 名字只取主行 `m.group(3)`，换行不拼回 | 主行无 EA 时向下拼接（跳过 `--`）；用"国家+数量"锚点 `([A-Z]{2})\s+\d+EA` 切名字边界并剥离国家码 |
| ② | 贵方零件号空，件号与订单号粘成一串 | `3506630Z473E(20230171K` 与 `P15158946` 无 2 空格间隔 | `re.split(r"\s{2,}")` 把粘连串当成一整列 | 取最右数字串作 Order 号，剩余用 `P\d{6,}` 锚点二次拆分 |
| ③ | 件号/订单号全空，Order 号取错 | 真正 Order 号前粘中文"出"（`出115943916`） | `fullmatch(r"\d{6,}")` 需整列纯数字，失配 | 改 `re.search`，允许前缀中文并剥离 |
| ④ | 件号丢失 + 数量错误 | 订单号含 `&` 双号（`4504675339 & 4504675`）；数量 `28.000EA` | 单空格 fallback 取错列；`(\d+)\s*EA` 截到 `000` | 数量正则改 `(\d[\d.,]*)\s*EA` 取整数；件号/订单号改"最右 Order 号 + 剩余列" |
| ⑤ | 非标准行号漏解析 | 6 位行号 / 单行版式 | `ITEM_LINE_RE` 只认 3 位行号 | 曾放宽后引发回归（见 2.3），最终收回 3 位并加"零件号必须含数字" |

### 2.3 二次回归修复（由回归工具发现）

放宽正则和"剥中文"逻辑曾引入 3 个新问题，均已修正：

| # | 文件 | 问题 | 根因 | 修法 |
|---|---|---|---|---|
| R1 | 77672982 | 韩国邮编 `17792 PYEONGTAEK` 被误判为项目行 | 行号放宽到 `\d{3,6}` 太宽 | 收回 `\d{3}` + 零件号必须含数字 `(?=[A-Z0-9]*\d)` |
| R2 | 77946384 | 合法中文 `索赔免费订单` 被清空 | "剥尾部中文"过度 | 改为只吃紧贴数字的中文前缀 |
| R3 | 78203415 / 77946384 | 件号/订单号归错列 | "件号=最左列"写死 | 改按**表头列 X 坐标**判定归属（靠右→订单号，靠左→件号） |

### 2.4 关键正则变化

```python
# 项目主行：3 位行号 + 零件号必须含数字（滤掉地址城市名等纯字母）
ITEM_LINE_RE  = re.compile(r"^\s*(\d{3})\s+((?=[A-Z0-9]*\d)[A-Z0-9]{6,})\s*(.*)")
# 数量：允许小数/千分位（28.000EA -> 28）
QTY_RE        = re.compile(r"(\d[\d.,]*)\s*EA\b")
# 国家 + 数量锚点（切名字边界、剥国家码）
COUNTRY_QTY_RE = re.compile(r"([A-Z]{2})\s+(\d[\d.,]*)\s*EA\b")
# 贵方订单号常见形态（P + 数字），用于列贴列二次拆分
CUST_ORDER_RE = re.compile(r"P\d{6,}")
```

### 2.5 新增方法 `_item_col_x()`

读取项目表头中"贵方零件号 / 贵方定单号"的横向 X 坐标（结果缓存），
供子行按列位置判定字段归属（靠右→订单号，靠左→件号）。

## 三、config.yaml 新增 `move_to_bak` 开关

### 用途
控制解析成功后是否把 htm 从 `Input/` 移动到 `Input/BAK/`。
- `true`（生产）：解析后归档，避免重复处理
- `false`（开发）：保留在 Input，可反复解析同一批文件调试

### 配置写法（注意缩进与 `overwrite` 对齐）
```yaml
process:
  overwrite: false      # 是否覆盖已解析文件
  move_to_bak: true     # ✅ 新增：解析后是否把 htm 移到 BAK
```

### 代码接入要点
- 函数开头读取：`move_to_bak = config.get("process", {}).get("move_to_bak", True)`
- `process_all_htm_files()` 中 **两处** `shutil.move`（`[SKIP]` 分支和 `[OK]` 分支）都要用 `if move_to_bak:` 包住
- `false` 时日志输出 `[KEEP]`，`true` 时输出 `[MOVED]`

### 自测结果（move_to_bak=false）
```
[OK]   77590927.htm -> Output\77590927.json
[KEEP][OK] 77590927.htm 保留在 Input（move_to_bak=false）
...
Program finished
```
✅ 15 个文件全部 `[OK]`，全部 `[KEEP]`（无 `[MOVED]`），开关生效。

## 四、新增 regression_check.py（回归对比工具）

### 作用
用修复版解析器重跑存量 htm，与旧基线 JSON 逐字段深比对，把差异分为：
- `[FIX ]`：项目明细内差异 —— 预期修复，安全
- `[WARN]`：表头/公司字段差异 —— 潜在回归，需人工确认

### 关键设计（避免踩坑）
1. **自动定位脚本目录**（`get_base_path()`），去掉写死的本机绝对路径
2. **HTM 源目录**默认 `Input/BAK`（主程序把解析过的 htm 移到这里），不存在则回退 `Input`
3. **基线目录**默认自动探测 `Output/` 下的 `YYYYMMDD` 子目录（如 `20260605`），
   **不会误用 `Output/` 根目录里的最新结果**当基线
4. **防呆**：基线目录 == 输出根目录时给出醒目警告
5. 支持 `--base / --input / --baseline / --output-root / --parser / --csv` 命令行覆盖

### 用法
```bash
# 零配置：源=Input/BAK，基线=Output/<最新日期子目录>
python regression_check.py

# 显式指定基线
python regression_check.py --baseline "Output\20260605"
```

> ⚠️ **重要约定**：旧基线 JSON 请放在 `Output/<日期>/` 子目录（如 `Output/20260605/`），
> 不要留在 `Output/` 根目录，否则会与最新结果混淆。

## 五、验证结果

### 5.1 新批次（16 单据 / 91 项目行）
- 解析成功率 **16/16（100%）**，零 FAIL / 零异常
- 关键字段（零件号 / 名字 / Our Order No.）**无一为空**
- 5 类修复在新数据上全部生效（`&` 双号、中文粘连、名字跨行、小数数量、列坐标判定）

### 5.2 老样本回归（5 个）
- 重新解析与已确认的正确结果**逐字段一致（5/5 PASS）**，未改坏

### 5.3 需人工确认 1 处
- **77535479 行 080/090**，名字解析为 `+++`
  - 零件号 `4410501200`、贵方件号 `22908A00054` 均正常，仅名字是 `+++`
  - 需核对源文件：若原单描述栏本就是 `+++`（占位/保密）则正常；若有真实品名则需再修

### 5.4 质检误报（无需处理）
- 名字尾部字母（`20L`/`24V`/`Basic 24V U`）：容量/电压/源文件定宽截断，非错误
- 行号跳号（`040→060` 等）：ZF 单据位号本就非连续，属原生特征

## 六、打包成 EXE（PyInstaller）

### 一键打包
项目根目录双击或执行 `build.bat`：
```bat
build.bat
```
内部命令等价于：
```bat
rmdir /s /q build dist __pycache__
del *.spec
pyinstaller --clean --onefile --name htm_parser htm_parser.py
```
产物：`dist\htm_parser.exe`

### 若提示 pyinstaller 未找到
```bat
pip install pyinstaller
```

### ⚠️ 打包后必做（--onefile 不含配置与目录）
`get_base_path()` 已兼容 exe（取 `sys.executable` 所在目录），需保证运行目录结构：
```
dist\
 ├─ htm_parser.exe
 ├─ config.yaml      ← 从根目录复制过来
 ├─ Input\           ← 放待解析 htm
 └─ Output\          ← 输出 json（旧基线放 Output\<日期>\ 子目录）
```

> 打包生产版前，建议把 `config.yaml` 的 `move_to_bak` 改回 `true`。

## 七、后续可选优化

- [ ] `build.bat` 升级：打包后自动复制 `config.yaml` 并建好 `Input/Output` 空目录
- [ ] `+++` 类占位名字的兜底逻辑（识别纯符号名字时回退取其他描述）
- [ ] 单行 / 6 位行号版式：补真实样本后做专项适配
- [ ] 把"基线放日期子目录"的约定固化到本文档回归用例中

---

*最后更新：2026-08-26*
